---
import BaseLayout from "./BaseLayout.astro";
import Header from "@/components/layout/Header.astro";
import Footer from "@/components/layout/Footer.astro";
import BackToTop from "@/components/interactive/BackToTop.astro";
import NocturneBackdrop from "@/components/layout/NocturneBackdrop.astro";

interface Props {
  title?: string;
  description?: string;
  ogType?: "website" | "article";
  ogImage?: string;
  publishedTime?: string;
  tags?: string[];
}

const { title, description, ...seoProps } = Astro.props;
---

<BaseLayout title={title} description={description} {...seoProps}>
  <div class="page-shell">
    <NocturneBackdrop />
    <Header />
    <main class="page-main">
      <slot />
    </main>
    <Footer />
    <BackToTop />
  </div>
</BaseLayout>

<style>
  .page-shell {
    display: flex;
    flex-direction: column;
    min-height: 100dvh;
  }

  .page-main {
    position: relative;
    z-index: 1;
    flex: 1;
  }

  :global(.footer) {
    position: relative;
    z-index: 1;
  }
</style>
